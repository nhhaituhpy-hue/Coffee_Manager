import { DailyEntry, FixedExpense, SavingTransaction } from './types';

export const MOCK_DAILY_ENTRIES: DailyEntry[] = [];

export const MOCK_FIXED_EXPENSES: FixedExpense[] = [];

export const MOCK_SAVINGS: SavingTransaction[] = [];
